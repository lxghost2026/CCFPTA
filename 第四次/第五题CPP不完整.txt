//计算超过d[i]的话费情况
int f(int id)
{
   return c[id]*(d[id]+1);
}

int main()
{
   std::vector<int> c={1,2,5,10};//硬币的面值

   int MAXN=100003;

   std::vector<long long> dp(MAXN+1,0);
   dp[0]=1;

   ///计算背包计算无线种的方案数
   for(int i=0;i<c.size();i++)
   {
      for(int j=c[i];j<=MAXN;j++)
         dp[j]+=dp[j-c[i]];
   }

   std::vector<int> d={3, 2, 3, 1};
   int s=10;//价值
   long long ans=dp[s];//设置为全部的方案数量
   //正确的方案数=全部的方案数-不正确的方案数







   system("pause");
   return 0;
}